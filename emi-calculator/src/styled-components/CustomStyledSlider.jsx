import { Slider, styled } from "@mui/material";

export const CustomStyledSlider = styled(Slider)(
  {
    "& .MuiSlider-mark":{
      height: '20px',
      top: '27px',
      bgColor:'black',
    }
  }
)
